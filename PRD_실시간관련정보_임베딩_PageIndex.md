# PRD — 실시간 관련정보 검색 부활 + 임베딩/PageIndex 기반 누적 검토

문서 버전: v1.1 (Phase 0 구현 완료 + 코드 리뷰 결함 4건 수정 반영)
작성일: 2026-07-28 · 갱신: 2026-07-29
작성: 심아름 (한빛솔루션)
대상 제품: Meeting Minutes Generator v2.1 (실시간 STT → 회의록 · Wiki)
문서 목적: **회의 중 관련 위키 노트를 실시간으로 찾아 보여주던 기능**의 현황을 정리하고,
**PageIndex**(구조 트리 인덱스)와 **turbovec**(로컬 벡터 인덱스)를 얹어
**"보이고 · 누적되고 · 다시 검토할 수 있는"** 형태로 복원·고도화하기 위한 요구사항을 정의한다.

## 구현 현황 (2026-07-29)

- **Phase 0 구현 완료** — FR-1·3·4·5·6·10·11·12 + FR-13(구현O·판정 보류). 신규 의존성 0개.
  커밋 `191b3f5`(부활·사유노출) → `7c9e467`(비방해 표시·웹은 보완재) → `02a4244`(누적·재열람·회의록 병합)
  → `0926625`(문서·스모크 절차) → `4e8e759`·`64c3819`(중복 칩 제거 · **랭킹 휴리스틱 2개 실측 반박·교체**).
  회귀 테스트 포함 전체 스위트 489 passed / 1 skipped.
- **코드 리뷰 후속 수정(2026-07-29)** — 아래 4건은 v1.0 이 ✅ 로 판정했지만 **실효가 없었다.**
  실측으로 확인하고 고쳤다. 재측정 결과 shipped 랭킹 지표는 전부 불변(R@3 .96 / MRR .920),
  구현·벤치 일치 24/24.
  1. **논문 보강 arm 이 폴더 3개 중 1개에서만 동작했다.** `search(path_prefixes=)` 는 접두사만
     매칭하는데 배지 판정은 경로 중간 일치를 해서, `02_이론_학습`(74노트)·`원문추출`(9노트)이
     이 arm 에서 **영구히 0건**이었다 → 두 규칙을 `VaultIndexer.path_matcher()` 한 곳으로
     합치고 arm 은 `path_match="segment"` 사용. 상세: `docs/검색랭킹_이론과근거.md` §5.7.
  2. **"동점이면 논문 우선" tie-break 가 죽은 코드였다.** `rank_score` 는 유일한 순위의 단조
     변환이라 동점이 발생할 수 없다 → 제거. 논문 우대를 되살리는 것은 §5.1 에서 반박된
     '가산' 방향이라 **의도적으로 복원하지 않는다.** 노출 보장은 arm ②가 전담.
  3. **FR-6 의 "임계값 필터"가 미작동이었다.** `rank_score`(상한 0.0164) 하한이라 점수처럼
     보이는 값을 주면 전부 걸러지는 함정 + 설정 노출 없음 → 순위 기반
     `wiki.related_notes_max_rank`(기본 0=끔)로 교체하고 UI 에 노출.
  4. **누적이 표시용 중복 제거에 잠식됐다.** 제목 dedup 을 검색 단계에서 해서 동명의 서로
     다른 노트가 누적·사이드카에서도 사라졌다 → dedup 을 **표시·회의록 단계로 이동**,
     누적은 전량. 프런트 dedup 키도 filename → title 로 맞춰 중복 칩을 실제로 없앴다.
  함께: REST 폴백에 남아 있던 논문 1.2배 가산 제거, 웹 보완 쿼리 60자 하드코딩 → 설정 반영,
  CLI "활성" 오출력 수정, 출처 아이콘·RRF 상수 중복 정의 통합,
  `realtime_paper_dirs`/`realtime_query_chars`/`realtime_search_backend` 설정 UI 노출.
- **미실시: FR-2 라이브 마이크 검증** — 절차는 `docs/SMOKE_실시간_관련노트.md`. 사람이 실제로
  녹음해야 하는 항목이라 `TODO.md`의 미해결 항목으로 남아 있다(수용기준 1·3·13도 여기에 묶임).
- **미착수: Phase 1** — FR-7(트리 노드 요약)·FR-8(ANN 백엔드)·FR-9(LLM 재랭킹)·FR-14(벡터 휠 번들).
  전환 트리거는 "노트 수"가 아니다 — `docs/검색랭킹_이론과근거.md` §7 참고.
- ⚠️ **이 문서의 §5.2·FR-11이 처음 규정한 검색 순서는 실측으로 반박돼 교체됐다**(아래 본문 반영).
  실제 채택안·수치·측정 한계: `docs/검색랭킹_이론과근거.md`, 재현: `scripts/bench_realtime_ranking.py`.
- 구현·설정 정본 문서: `docs/ARCHITECTURE.md`("실시간 관련 노트" 절 + wiki 설정표), `Readme.md`,
  `docs/회의비서_사용법.md`, `docs/RELEASE_NOTES_최신.md`.

> 채택 오픈소스 (냉철한 위치 정리 — 상세 §5.0):
> - **PageIndex** — https://github.com/VectifyAI/PageIndex (MIT). 트리(ToC) 구조 인덱스 아이디어를
>   **차용**(의존성 강제 아님). 이미 있는 `search_sections` 섹션 인덱스에 노드 요약을 더하는 방식.
> - **turbovec** — https://github.com/RyanCodrai/turbovec (MIT). TurboQuant Rust 벡터 인덱스
>   (`cp39-abi3-win_amd64` 휠 확인 → 3.13 포터블 번들 가능). **Phase 1 옵션** — 벡터가 수천~수만으로
>   커질 때의 로컬 ANN 백엔드. 현재 규모(실측 472노트·3,744섹션)에선 numpy 브루트포스로 충분해
>   **v1 블로커 아님** — 검색 1회의 **로컬 계산이 89ms**(벡터 코사인은 그중 수 ms)로 확인됐고,
>   실제로 도입하지 않았다. 실사용 지연 0.3~0.5초는 대부분 쿼리 임베딩 API 왕복이라 ANN 으로는
>   줄지 않는다.

---

## 1. 배경 — "그 기능이 어디 갔나"

결론(착수 시점 진단): **기능은 사라지지 않았다. 구현·배선까지 끝나 있었으나 기본값이 꺼져 있었고
(default OFF), 실환경 라이브 검증이 안 돼 화면에서 안 보였을 뿐이다.**
→ Phase 0에서 기본값 ON + 사유 노출로 해소했다(라이브 검증만 잔여 — 아래 3번).

근거(코드 기준):

- 공유 모듈 `meeting_minutes_app/wiki_core/realtime_search.py` 에 `RealtimeVaultSearcher`
  클래스가 존재한다. 회의 중 발화 세그먼트가 확정될 때마다 관련 vault 노트를 **논블로킹**
  백그라운드로 검색해 호출자에게 전달한다.
- CLI 경로: `meeting_pipeline/realtime_transcription.py:1495` 에서 생성·사용.
- 웹 경로: `web/backend/api/realtime.py:671`(`_make_vault_searcher`) 에서 생성,
  `:685 _emit_related_notes()` 가 WebSocket으로 `type: "related_notes"` 이벤트를 브라우저에 push.
- 웹 UI: `web/frontend/src/components/Recorder.tsx` 의 **관련 노트** 바
  (`:420 related_notes` 핸들러, `:1048~` 렌더링)가 관련 노트를 `[[제목#섹션]]` 칩으로 표시.
- 도입 시점: 커밋 `d03657d` ("feat: unify finalize pipeline ... + realtime search").

**왜 안 보였나 (핵심 원인 3가지)** — 재발 방지용 이력. 1·2는 해소, 3은 미해소:

1. ~~**기본 OFF.**~~ **해소(`191b3f5`)** — `config_schema.py:180` `wiki.realtime_vault_search`
   default를 `False` → **`True`** 로 승격. 이전에는 Settings에서 켜지 않으면 `offer_segment()`가
   전부 no-op이라 아무것도 안 떴다.
2. ~~**조용한 비활성화.**~~ **해소(`191b3f5`/`7c9e467`)** — 로컬 인덱스(`VaultIndexer`)도 Obsidian
   REST도 못 붙으면 여전히 `_disabled=True`로 스트림을 보호하지만, 이제 사유를
   `status()`(`off`/`no_vault`/`index_missing`/`obsidian_unreachable`/`no_backend`)로 노출해
   웹은 배지, CLI는 안내 줄로 보여준다. `warmup()`으로 첫 발화 전에 판정한다.
3. **라이브 미검증 — 아직 미해소.** `TODO.md`에 "실시간 CLI/웹 녹취 라이브 테스트 미실시"로
   남아 있다(마이크 실녹음 기준). 절차만 `docs/SMOKE_실시간_관련노트.md`로 준비됨.

---

## 2. 현재 구현 스냅샷 (있는 것 / 없는 것)

### 2.1 이미 되어 있는 것 ✅

| 영역 | 현황 |
|---|---|
| 실시간 검색 엔진 | `RealtimeVaultSearcher` — 세그먼트 스로틀(기본 3세그먼트마다), 논블로킹 스레드풀, 예외 전파 0 |
| 백엔드 우선순위 | `VaultIndexer`(로컬 인덱스, ms 단위) 우선 → Obsidian REST 폴백 → 둘 다 실패 시 비활성 (`wiki.realtime_search_backend`: auto/index/rest) |
| **임베딩(의미검색) 이미 존재** | `vault_indexer.py`의 `search()`는 TF-IDF 기본, `wiki_knowledge.embedding_enabled=true`(**default True**)면 TF-IDF + 임베딩 코사인을 **RRF 융합**. 모델 `text-embedding-3-small`, dims 256, `embedding_min_cosine` 0.25 |
| 세션 내 누적 | 프론트 `Recorder.tsx`가 `related_notes`를 이전 결과와 **머지·dedup**(filename/title 기준)해 세션 동안 축적, 상위 8개 + `+N` 표시 |
| 종료 후 병합 훅 | `collected_titles()` — 세션 종료 후 회의록 memo에 관련 노트 제목 병합용 API 존재 (Phase 0에서 근거까지 넘기는 `collected_evidence()` 추가) |
| 스팸 억제 | 상위 3개, 직전과 동일 노트 세트면 표시 생략(`_last_shown_titles`) |
| **섹션(heading) 인덱스 이미 존재** | `vault_indexer.py:313` `wiki_knowledge.section_index_enabled`(**default True**) → 노트를 `#` 헤딩 단위 섹션으로 인덱싱, `search_sections()`(`:779`)로 heading 단위 검색. 인용 정확도용. **PageIndex 트리의 얕은 버전이 이미 있음** |
| **내부자료 우선 검증 이미 존재** | `meeting_workflow.claim_verify()`(`:620`) — 주 도메인 주장은 **vault 우선**(`_gather_claim_vault_context` "오프라인 우선", 섹션 인덱스 우선), vault로 불확실할 때만 논문(`_paper_verify_claim`, arXiv 우선)·웹으로. 도메인 외 웹검증 결과는 참고노트로 **축적**(다음 회의부터 로컬 검증) |
| 실시간 웹검색(웹 전용) | `realtime.py:741~`(`_maybe_web_research`) — `wiki.online_search_enabled`+`wiki.realtime_web_search_interval`>0일 때만 발화별 `web_research`. **CLI 실시간엔 없음**(FR-12에서 이 동작을 정본으로 확정) |
| 종료 후 병합 | `finalize.py:190` — 실시간 관련 노트 제목 + 웹검색 보완 블록을 회의록 memo에 병합 |

### 2.2 빠져 있던 것 / 약했던 것 (이번 PRD 대상 → **Phase 0에서 전부 해소**)

1. ~~**기본 OFF + 실패 시 무피드백**~~ → **해소**: default `True` + 사유 배지/안내 줄(§1 참고).
2. ~~**누적이 세션 휘발성**~~ → **해소(`02a4244`)**: 종료 시 `collected_evidence()`(노트별 최고 근거
   + 참조 횟수)를 웹 SQLite `related_notes` 테이블에 upsert(`web/backend/database.py:78`),
   `GET /api/sessions/{id}/related-notes`(`api/sessions.py:62`) + 회의 상세 "참조된 관련 노트" 카드
   + 교차 회의 집계(`related_notes_cross_sessions()`)로 재열람. 근거(score/snippet/섹션/발화)도 보존.
3. ~~**검색이 "짧은 키워드" 중심**~~ → **완화**: 쿼리를 앞 60자 → **180자**(`wiki.realtime_query_chars`)로
   늘리고(긴 문맥이 임베딩·교차언어 회수에 유리), 후보 노트 안에서 `sections_in_notes()`로 **어느
   대목이 근거인지**를 특정한다(heading 정확도 0 → 0.87). 다만 이는 **후보 내 위치 특정**이고,
   섹션을 랭킹 축으로 쓰는 트리 라우팅(FR-7/8)은 Phase 1로 남았다 — 실측상 랭킹 arm 융합은
   회수 이득 없이 지연만 5배였다(§5.2).
4. ~~**표시가 칩 뿐**~~ → **해소(`7c9e467`)**: '근거 보기'를 펼치면 섹션경로·score·일치유형(섹션/노트/웹)
   ·snippet·해당 발화가 보인다(`Recorder.tsx:1107~`). 노트 이동은 **녹음 중에는 제공하지 않는다** —
   Recorder 언마운트가 곧 녹음 중단이므로, 종료 후 회의 상세에서 제목 클릭으로 연다(의도적 설계).

---

## 3. 목표 / 비목표

### 3.1 목표 (Goals)
- **G1.** 회의 중 관련 노트가 **실제로 보인다** — 기본 동작·실패 사유 노출로 "사라짐" 재발 방지.
- **G2.** 관련정보가 **회의 간 누적**되어, 회의가 끝난 뒤에도 근거와 함께 **다시 검토**할 수 있다.
- **G3.** PageIndex식 **구조(트리) 인덱스 + 임베딩**을 얹어, 유사도가 아닌 **관련성(reasoning)** 기반으로
  긴 문서에서 관련 섹션까지 짚는다. 검색 결과는 **추적 가능(traceable)** 하게.
- **G4.** 사내 배포 제약(오프라인 가능·저비용) 안에서 **빠르게 적용** 가능한 경로를 우선한다.
- **G5.** **내부자료(Obsidian/.md·논문 노트)를 웹보다 먼저·꼼꼼히** 찾고, 표시는 **전사를 방해하지 않는다.**
- **G6.** 영어·한국어 발화 모두, meeting·seminar·lecture 모든 유형, 웹·터미널(dist) 모든 실행에서 동작한다.

### 3.2 비목표 (이번 범위 밖)
- 실시간 STT 파이프라인 자체 개편(별도 트랙).
- 외부 클라우드 벡터 DB 상시 의존(사내망/오프라인 요건과 충돌 가능 — §5에서 옵션으로만).
- Vault 원본 노트 자동 편집(관련정보는 별도 사이드카/DB에 축적, 원본 불변 원칙 유지).

---

## 4. 요구사항

### 4.1 P0 — 기능 복원 & 신뢰성 (먼저)

**FR-1 실시간 검색 가시성/상태 노출** — ✅ 완료 (`191b3f5`)
Settings의 "실시간 볼트 검색" 토글을 실사용 기본 경로로 승격하고, 켰을 때 백엔드가
붙었는지/비활성됐는지(인덱스 없음·Obsidian 미연결 등)를 Recorder 화면에 **상태 배지**로 표시한다.
(현재는 조용히 no-op → 원인 불명. `realtime_search._disabled` 사유를 이벤트로 전달.)

**FR-2 라이브 스모크 검증** — ⚠️ **절차만 완료(`0926625`), 실기 검증 미실시**
CLI/웹 양 경로에서 실제 마이크 녹음으로 `related_notes`가 뜨는지 확인하는 스모크 절차를
`tests/` 또는 `docs/`에 추가하고 `TODO.md`의 미검증 항목을 해소한다.
→ 절차는 `docs/SMOKE_실시간_관련노트.md`(웹/CLI/문서유형/진단표). **사람이 마이크로 실제
녹음해야 하는 단계는 아직 실행되지 않았다** — `TODO.md` 미해결 항목 유지. 자동 테스트는
검색·표시·누적 로직만 덮는다(`tests/test_realtime_search.py`, `tests/test_realtime_related_notes.py`).

**FR-3 근거 함께 표시** — ✅ 완료 (`7c9e467`)
칩만이 아니라 각 관련 노트에 **점수·매칭 snippet·출처 경로**를 함께 보이고, 클릭 시
해당 노트로 이동(웹: 노트 뷰/Obsidian 링크, CLI: 경로 출력). "왜 떴는지"를 추적 가능하게.
→ 구현: 웹은 '근거 보기' 펼침에 섹션경로·score·일치유형·snippet·발화, CLI는 칩 다음 줄에 노트 경로.
**노트 이동만 정책 변경** — 녹음 중 이동은 Recorder 언마운트=녹음 중단이라 제공하지 않고, 종료 후
회의 상세 '참조된 관련 노트'에서 제공한다(화면에도 그 사실을 한 줄로 안내).

### 4.2 P1 — 누적 검토 (핵심 확장)

**FR-4 회의 간 누적 저장 (사이드카)** — ✅ 완료 (`02a4244`, SQLite 부분만)
세션 동안 수집된 관련 노트를 근거(세그먼트 텍스트·점수·snippet·시각·회의 id)와 함께
**영속 저장**한다. 메타데이터는 SQLite(`web/meeting_assistant.db`), 벡터는 turbovec 인덱스(`data/*.tvim`)에
**온라인 ingest**(train/rebuild 없이 add만) — 회의가 쌓일수록 인덱스가 자연 성장한다. 원본 vault는 불변.
→ 구현: SQLite `related_notes` 테이블(session_id+note_path upsert, hits 누적, `idx_related_path`).
**벡터 사이드카(`*.tvim`)는 Phase 1** — 현 규모에선 불필요(§5.0).

**FR-5 누적 검토 뷰** — ✅ 완료 (`02a4244`)
회의별 "이번 회의에서 참조된 관련 노트" 목록 + **교차 회의 집계**("이 노트가 최근 N개 회의에서
몇 번 참조됐나")를 볼 수 있는 화면/CLI를 제공. 발화 맥락(세그먼트)과 함께 되짚어볼 수 있어야 한다.

**FR-6 회의록 자동 병합 강화** — ✅ 완료 (`02a4244`)
현재 `collected_titles()`(제목만) → **제목 + 근거 링크**를 회의록의 `🔗 관련 노트` 섹션에
자동 삽입(위키 규약의 출처 표기와 정합). 중복/노이즈는 임계값으로 필터.
→ 구현: `finalize`가 근거를 생성 memo에 주입하고, 회의록 말미에 `## 🔗 관련 노트`(링크+관련도+참조
횟수)를 **LLM 없이 결정적으로 append**. 위치는 **사실검증 블록 뒤** — 검증 섹션 재작성에 지워지지 않게.
중복은 **제목 기준**으로 합치고(같은 제목의 다른 경로 노트는 회의록에서 구분되지 않으므로),
노이즈는 **순위 상한** `wiki.related_notes_max_rank`(기본 0=끔)로 컷한다.
⚠ 2026-07-29 리뷰까지 이 임계값은 `rank_score`(상한 0.0164) 하한이었고 설정 노출도 없어
**사실상 미작동**이었다 — 순위 기반으로 교체하고 UI에 노출했다.

### 4.3 P2 — PageIndex식 구조 인덱스 (관련성↑) — ❌ **Phase 1 미착수 (FR-7/8/9 전부)**

> 착수 조건은 노트 수가 아니다. `docs/검색랭킹_이론과근거.md` §7의 전환 트리거
> (섹션 트리 요약이 실제로 필요한 긴 보고서 유입 / 완전 오프라인 요구 / 벡터 수천~수만)를 먼저 본다.
> Phase 0에서 확인된 사실: **섹션을 랭킹 축으로 융합하는 것은 이득이 없었다**(§5.2) — FR-8을
> 착수할 땐 "노드 요약 임베딩"이 그 결론을 뒤집는지부터 실측해야 한다.

**FR-7 트리(ToC) 인덱스 생성**
vault의 긴 노트(회의록·보고서)를 마크다운 헤딩(`#`/`##`/`###`) 기준 **계층 트리**로 인덱싱하고,
노드별 요약을 생성한다. PageIndex의 markdown 모드(`--md_path`, `#` 레벨→노드)와 동형.
→ 산출물은 노트당 `{title, node_id, summary, 범위}` 트리(JSON).

**FR-8 트리 노드 임베딩 + 하이브리드 라우팅**
노트 단위 임베딩(현행)에 더해 **섹션(노드) 요약 임베딩**을 추가해 관련 "섹션"까지 라우팅한다.
- **Phase 0(기본):** 후보를 `search_sections`로 좁힌 뒤 **numpy 브루트포스 코사인**으로 재랭킹
  (현 `_semantic_ranking` 확장). ≤수천 벡터에서 즉답이라 별도 ANN 불필요.
- **Phase 1(옵션):** 벡터가 커지면 **turbovec `IdMapIndex`** 로 전환 — node_id를 uint64 외부 id로
  매핑(O(1) 삭제/갱신), `search(query_vec, k, allowlist=후보ids)` 로 커널 내부 필터, `data/*.tvim` 영속.
결과에 `섹션경로/score/snippet`을 실어 FR-3 traceability를 강화(두 Phase 공통).

**FR-9 (선택) reasoning 기반 재랭킹**
후보 노드에 대해 LLM이 "관련성"을 판정하는 경량 재랭킹(PageIndex의 tree-search 축약형).
비용/지연 때문에 **실시간이 아니라 종료 후 누적 검토(FR-5)에서만** 적용, 기본 off.

### 4.4 P0/P1 — 표시·검색 정책 (이번 완성본에서 확정)

**FR-10 비방해(non-intrusive) 웹 표시** *(P0, 요청 반영)* — ✅ 완료 (`7c9e467`, `4e8e759`)
실시간 관련정보는 **전사 흐름을 절대 가리거나 밀어내지 않는다.** 현행 Recorder의 상단 **관련 노트**
가로 칩 바(구현 후 `Recorder.tsx:1048~`)를 기준으로:
- 위치: 전사 본문 **밖의 주변 영역**(상단 얇은 바 또는 접이식 우측 사이드 패널). 본문 레이아웃 리플로우 금지.
- 갱신: 조용히 교체(현행 `_last_shown_titles` dedup 유지), 팝업·토스트·소리·포커스 이동 없음.
- 밀도: 기본 접힘/최소(칩 상위 N + `+N`), 사용자가 펼치면 근거(점수·snippet·섹션경로·출처유형) 표시.
- 출처 구분: **내부(📄 vault/md · 🎓 논문노트)** 와 **웹(🌐)** 을 배지로 시각 구분, 내부를 앞줄에 배치.
- 상태(FR-1)도 같은 바에 조용히 표시(붙음/비활성 사유). 실패해도 화면 정지·에러 팝업 없음.

→ 구현: 녹음 중 **고정 높이 얇은 바를 상시 유지**해 결과가 새로 들어와도 리플로우가 0이고, 근거
상세는 자체 스크롤(`max-h-44`)로 전사 영역을 잠식하지 않는다. 단독(standalone)/모바일 직접 연결
모드에서는 사이드카가 없어 바 자체를 숨긴다(`4e8e759`).

**FR-11 내부자료 우선 · 꼼꼼 검색** *(P0, 요청 반영)* — ✅ 완료 (`191b3f5` → **`64c3819`에서 순서 교체**)
관련정보는 **항상 내부자료(Obsidian/.md 노트 + 논문/이론 노트)를 먼저, 그리고 꼼꼼히** 찾는다.
- ~~순서: ① 섹션(heading) 인덱스 → ② 노트 인덱스(TF-IDF+임베딩 RRF) → ③ (부족 시에만) 웹.~~
  ⚠️ **이 초안 순서는 실측에서 반박됐다.** 확정 순서(구현 = `realtime_search._search_index`):
  ① **노트 인덱스 `search()`(TF-IDF+임베딩 RRF)** — 랭킹의 주축이자 교차언어 회수 담당 →
  ② **논문/이론 폴더 한정 노트 검색**(`wiki.realtime_paper_dirs`) — 로컬 논문이 상한에 밀려 후보에서
  빠지는 것을 막는 **보강 arm** → ③ `sections_in_notes()`로 **후보 노트 안에서만** 섹션 채점(랭킹
  불참, "어느 대목이 근거인가"만) → ④ 웹은 보완재. 순위는 **① 랭킹 순서 + ②에서만 나온 논문
  후보를 그 뒤에 이어붙임**. 논문은 **점수도 순위도 우대하지 않는다** — 초안의 "동점이면 논문
  우선(tie-break)"은 `rank_score` 가 유일 순위의 단조 변환이라 동점이 발생할 수 없어
  **발동 불가능한 죽은 코드였고, 2026-07-29 리뷰에서 제거**했다. 근거·수치는 §5.2.
  현행 `claim_verify`의 "vault 우선" 정책을 **실시간 경로에도 동일 적용**한다.
- 꼼꼼함: 실시간은 상위 3개 노출이지만 **내부 후보 풀은 넉넉히 수집·누적**하고(확정: 노트 10 +
  논문 4 = **최대** 14, `wiki.realtime_note_candidates`/`realtime_paper_candidates`), 표시만 상위로
  제한. 두 arm 이 같은 노트를 돌려주면 후보는 그만큼 줄어든다(중복 진입 없음).
  종료 후 누적 검토(FR-5)에서 전체 후보를 되짚을 수 있게 **누적은 전량**이고, 같은 제목을 하나로
  합치는 것은 **표시·회의록 단계에서만** 한다. 쿼리는 발화 앞 180자 + topic.
- 논문: vault의 `02_이론_학습`·`01_References`·`원문추출/` 등 **로컬 논문/원문 추출을 최우선** 검색.
  로컬에 없을 때만 arXiv 등 웹(`claim_paper_verify`)로 보완하고, 결과는 참고노트로 **축적**(로컬화).
  (경로: 이 볼트에서 `02_이론_학습`·`원문추출`은 루트가 아니라
  `Archive/도메인_아카이브/` 하위에 있고, 폴더명 **경로 중간 일치**로 매칭한다
  — `VaultIndexer.path_matcher(..., "segment")`. ⚠ 2026-07-29 리뷰까지 이 중간 일치는 **배지
  판정에만** 적용되고 보강 arm 은 접두사만 봐서 위 두 폴더 83노트가 arm 에서 영구히 0건이었다.
  지금은 두 경로가 같은 헬퍼를 공유한다 — `docs/검색랭킹_이론과근거.md` §5.7.
  루트 학습 폴더는 `06_Study`.)
- 웹은 **항상 보완재**: 기본 off, 내부에서 못 찾았을 때만(`wiki.realtime_web_only_if_no_vault_hit`,
  기본 true), 그리고 FR-10대로 내부 결과 뒤에 표시.

**FR-12 CLI 실시간 웹검색 정합** *(P1)* — ✅ 완료 (`0926625`) — **(b) 채택**
현재 실시간 웹검색이 웹 UI에만 있는 불일치를 정리한다. 택1로 확정:
(a) CLI 실시간에도 동일 게이트(`online_search_enabled`+`realtime_web_search_interval`)로 웹 보완 추가, 또는
(b) "실시간 웹검색은 웹 UI 전용"을 **문서/설정 설명에 명시**(CLI는 내부검색만). — 최소한 동작을 문서화한다.
→ **결정: (b).** 실시간 웹 보완은 웹 UI 녹음 전용이고 CLI는 내부 검색만 한다는 것을
`config_schema.py`(설정 설명) · `config.example.json`(주석) · `Readme.md` · `docs/ARCHITECTURE.md`
· `docs/SMOKE_실시간_관련노트.md` 다섯 곳에 명시. 추가로 `wiki.realtime_web_only_if_no_vault_hit`
(기본 true)를 신설해 웹은 내부 미발견 구간에서만 호출한다.

**FR-13 교차언어(영↔한) 검색** *(P1, 요청 반영)* — ⚠️ 구현 완료 · **판정 보류**
영어 발화로도 한국어 위키가, 한국어 발화로도 영어 자료가 검색되게 한다.
- 1차: 임베딩(다국어) 경로를 교차언어 후보에 활용(TF-IDF 단독의 토큰 불일치 보완).
- 2차: 섹션 노드 요약에 **양어 키워드**를 병기(요약 생성 시 원어+한국어 키워드)해 임베딩 대상
  텍스트를 교차언어 친화적으로.
- 주의(냉철): 현 `text-embedding-3-small`은 다국어이나 교차언어 정합이 완벽하진 않다. 스모크 통과가
  기준선이고, 부족하면 다국어 특화 임베딩(예: multilingual-e5)으로 교체 검토(Phase 1).
- 검증: en 발화 → 관련 ko 노트가 상위에 뜨는지 스모크(FR 수용기준에 포함).

→ 현황: 경로는 살아 있다(노트 RRF의 임베딩 축이 담당, 쿼리 180자로 문맥도 늘렸다). 실측에서
en 발화 → 한국어 노트 회수 자체는 확인됐지만 **정량 판정은 보류**한다 — 임베딩의 이득이 낡은
(중복 포함) 인덱스에서만 나타나고 정상 인덱스에선 사라졌고, 이는 합성 쿼리(원문 축자 발췌)가
어휘매칭에 유리한 **구조적 편향** 때문이다. 영→한은 확실히 약하다(R@3 0.62 vs ko 0.96).
판정에는 실제 전사 기반 **환언(paraphrase) 평가셋**이 필요하고, 그것이 이 트랙의 최우선 후속이다.
노드 요약 양어 키워드 병기는 FR-7과 함께 Phase 1.

**FR-14 (Phase 1) 벡터 백엔드 번들 & 자동 강등** — ❌ Phase 1 미착수 (현행이 곧 강등 경로)
Phase 1에서 ANN 백엔드를 도입할 때, 빌드(`build_portable.ps1`의 `pip install --target`)로 휠을
`Lib\site-packages`에 동봉한다. **turbovec `cp39-abi3-win_amd64` 휠 존재를 확인**했으므로 3.13
포터블에 번들 가능(대안: `sqlite-vec` py3-none 휠, 더 단순). **런타임 안전장치 필수**: turbovec가 없거나
**AVX2 미지원 CPU(2013년 이전)**·로드 실패 시 **numpy 브루트포스 경로로 자동 강등**(기능 정지·에러 없음).
이 강등 경로가 곧 Phase 0의 기본 경로이므로 추가 개발부담이 없다.

---

## 5. 채택 기술 & 적용 아키텍처 (냉철한 실현성 평가)

### 5.0 결론 먼저 (TL;DR)

**Phase 0 = 신규 의존성 0개로 지금 당장 구현 가능.** 이 PRD의 요청 핵심(기능 부활·비방해 표시·
내부자료 우선·누적 검토·교차언어)은 **이미 있는 스택**(OpenAI 임베딩 + `vault_indexer`의 TF-IDF/임베딩
RRF + `search_sections` 섹션 인덱스 + 웹 SQLite `meeting_assistant.db`)만으로 전부 만들 수 있다.
- 위키 규모(실측 정정, 2026-07-29 `reindex` 후): **472노트 · 3,744섹션**. (초안의 "약 108개"는
  큐레이션 노트만 센 값이고, 한때 보였던 802노트는 그림자 사본(`*.txt.md`)이 섞인 낡은 인덱스 값이다.)
  이 규모에서도 **numpy 브루트포스 코사인이 즉답**이다(`vault_indexer._semantic_ranking`) — 실시간
  검색 1회의 **로컬 계산이 89ms**로 측정됐다(벡터 코사인은 그중 수 ms). **ANN
  라이브러리(turbovec/faiss/…)가 없어도 된다**는 결론은 유지 — 실사용 지연 0.3~0.5초의 대부분은
  쿼리 임베딩 API 왕복이라 ANN 으로는 줄지 않는다.
- PageIndex도 **의존성으로 넣을 필요 없다.** 이미 `#` 헤딩을 섹션으로 쪼개는 `_split_sections`/
  `search_sections`가 있으므로, PageIndex의 *아이디어*(트리·노드 요약)만 차용해 기존 섹션 인덱스에
  노드 요약을 더하면 된다.

**Phase 1 = 선택적 고도화(규모/오프라인 요구가 생길 때).** turbovec·로컬 임베딩·PageIndex Flash는
"수천~수만 벡터로 커지거나, API 없이 완전 오프라인이 필요할 때" 켜는 **옵션**이다. v1 배포의 블로커가 아니다.

### 5.1 Windows 내장(오프라인) vs API — 실측 배포성

> 실측: `pip download --only-binary --python-version 313 --platform win_amd64` 로 cp313/abi3 휠 존재 확인(2026-07-28).

| 후보 | 역할 | Windows 포터블(embed py3.13) | 판정 |
|---|---|---|---|
| **현행 numpy 브루트포스** | 벡터 코사인(≤수천 벡터) | 이미 번들됨 | **Phase 0 기본** — 신규 0 |
| **turbovec** (MIT) | ANN 벡터 인덱스 | ✅ `cp39-abi3-win_amd64` 휠(666KB) 존재 → 3.13 설치 가능. 단 **AVX2(2013+ CPU)** 필요 | Phase 1 옵션 |
| **sqlite-vec** (MIT) | SQLite 확장 KNN | ✅ `py3-none-win_amd64`(292KB), 초경량. 브루트포스 KNN(ANN 아님) | Phase 1 대안(가장 단순) |
| faiss-cpu | ANN | ✅ cp313 휠(16MB) | 무거움, 비권장 |
| hnswlib | ANN | ❌ **휠 없음(소스만)** → C++ 컴파일 필요 | 포터블 부적합, 제외 |
| lancedb / chromadb | 임베디드 벡터DB | ✅ 휠 있으나 **63MB / 23MB** | 과대, 제외 |
| **model2vec** (MIT) | **로컬 임베딩**(정적, CPU 초고속) | ✅ `py3-none-any`(54KB) + 모델 1회 다운로드 | Phase 1 오프라인 임베딩 |
| fastembed | 로컬 임베딩(ONNX) | ✅ 휠 있으나 onnxruntime 등 무거움·모델 다운로드 | Phase 1 대안(무거움) |
| **OpenAI `text-embedding-3-small`** | 임베딩(API) | — (네트워크) | **Phase 0 기본**(이미 사용 중) |
| **web_search**(Anthropic→GPT) | 웹 보완/논문(API) | — (네트워크) | 이미 사용 중, 보완재 |

핵심: **Phase 0는 API(OpenAI 임베딩·web_search) + 이미 번들된 numpy** 로 충분. turbovec/sqlite-vec/
model2vec 는 전부 **Windows 휠이 있어 포터블 번들 가능**하지만(hnswlib만 불가), **지금 규모엔 불필요**.

### 5.2 검색 파이프라인 (실시간) — **구현 확정본** (초안 순서는 실측으로 반박됨)

```
발화 세그먼트 ─▶ 쿼리 = 발화 앞 180자 + topic      (3세그먼트마다, 논블로킹 풀)
   │
   ├─ arm ①: 노트 인덱스 search(k=10)      TF-IDF + 임베딩 코사인 RRF   ← 랭킹 주축·교차언어
   ├─ arm ②: 같은 search(k=4, path_prefixes=realtime_paper_dirs, path_match="segment")
   │        → 최대 14건, 순위 = ① 랭킹 순 + ②에서만 나온 논문을 뒤에. 논문 우대 없음
   ├─ 위치특정: sections_in_notes(후보 ≤14건만)  → heading·snippet (랭킹 불참, ~0.5ms)
   ├─ 누적은 **전량**(경로 단위) / 표시는 dedupe_by_title 후 상위 3
   ├─ (웹 UI 전용·기본 off): 내부 후보 0건인 구간에서만 web_search → 내부 결과 뒤에 표시
   │
   └─▶ {note, 섹션경로, score, rank, rank_score, snippet, 출처유형, 발화}
        ─▶ 비방해 표시(FR-10) + SQLite 누적(FR-4)
```

**초안(§4.4 FR-11의 "섹션 1차 → 노트 2차")을 버린 근거** — 실측 2026-07-29, 정상 인덱스
472노트·3,744섹션, 합성 쿼리 n=24 (`scripts/bench_realtime_ranking.py`로 재현):

(아래는 arm ② 폴더 매칭 수정 후 재측정본. 지표는 수정 전과 동일하고 절대 지연만 달라졌다.)

| 안 | R@1 | R@3 | MRR | heading 정확도 | 로컬 지연 |
|---|---|---|---|---|---|
| **채택(노트 RRF + 논문 arm + 후보내 섹션)** | .88 | **.96** | **.920** | **.87** | **89ms** |
| 논문 폴더 1.2배 가산 | .62 | .71 | .713 | — | 105ms |
| 볼트 전체 섹션을 랭킹 arm으로 융합 | .79 | .96(동일) | .880 | .78 | 240ms |

- **논문 가산은 랭킹을 망친다**(MRR .920→.713). 낡은 802노트 인덱스에서도 방향이 같았다
  (.664→.575) — 두 인덱스에서 일관되게 재현된 **가장 강한 결론**. 폴더 소속은 관련도의 근거가
  아니므로 **순위 우대는 아예 하지 않고**(tie-break 포함), 후보 진입만 arm ②가 보장한다.
- **섹션 arm 배제 근거는 "회수 악화"가 아니라 지연 2.7배 + heading 정확도**다. 섹션 TF-IDF는 tf
  길이정규화 때문에 짧은 섹션이 과대평가돼 노트 점수와 같은 축에 둘 수 없다. 그래서 **역할 분리**
  (노트=무엇을, 섹션=어디를). 섹션을 아예 안 쓰면 heading 정확도가 0이 된다.
- ⚠ **지연은 로컬 계산만의 값이다.** 기본 설정에선 검색마다 쿼리 임베딩 API 왕복(~270ms)이
  붙어 실사용 1회는 **0.3~0.5초**다. 전용 워커 스레드라 전사 스트림에는 영향이 없다.
- 측정 한계: 합성 쿼리(원문 축자 발췌)는 어휘매칭에 유리한 편향이 있다 → 임베딩·교차언어 판정은
  보류(FR-13). 낡은 인덱스로 재면 수치가 통째로 달라지므로 **측정 전 `reindex` 필수**.
  전체 수식·코드 대조·재검증 대상: `docs/검색랭킹_이론과근거.md`.

- 실시간 경로엔 LLM reasoning 미투입(지연·비용). LLM 재랭킹(FR-9)은 종료 후 누적 검토에서만 옵션.
- 누적 검토(FR-5)는 SQLite에 회의 id·근거로 저장 → "이 회의/최근 N회의" 시간창 질의로 재사용
  (Phase 1에서 turbovec 도입 시 allowlist로 동일 구현).
- ~~잔여 불일치: REST 폴백(`_search_rest`)에 논문 1.2배 가산 잔존.~~ **해소(2026-07-29 리뷰)** —
  제거해 두 백엔드가 같은 순위 규칙을 쓴다. 회귀 테스트
  `test_rest_fallback_gives_papers_no_boost` 로 고정.

### 5.3 권장 적용 순서 (실행 계획)

**Phase 0 — 신규 의존성 0, 지금 구현 (v1)** — 1~5 완료, 6 미실시
1. ✅ **FR-1** 기능 ON + 상태 배지 (config 기본값·사유 노출). 최소 코드.
2. ✅ **FR-11** 실시간 경로 내부자료 우선·꼼꼼화 — 단 **순서는 노트 RRF 주축 + 논문 arm + 후보내
   섹션 위치특정**으로 확정(초안의 `search_sections` 우선은 실측 반박, §5.2).
3. ✅ **FR-10** 비방해 웹 표시 정비(칩 바 접이식·출처 배지·논팝업), **FR-3** 근거/섹션경로
   (노트 이동은 종료 후 상세에서 — 녹음 보호).
4. ✅ **FR-4/5/6** SQLite 사이드카 누적 저장 + 검토 뷰 + 회의록 근거 병합.
5. ✅ **FR-12** CLI 웹검색 정합/문서화 · ⚠️ **FR-13** 교차언어(경로는 동작, 정량 판정 보류 —
   환언 평가셋 필요. 노드 요약 양어 키워드는 Phase 1).
6. ⚠️ **FR-2** CLI/웹 라이브 스모크 검증 — 절차만 준비(`docs/SMOKE_실시간_관련노트.md`), **실기 미실시**.

**Phase 1 — 선택적 고도화 (규모↑ 또는 완전 오프라인 요구 시)**
7. 섹션 노드 요약 생성(PageIndex Flash 차용 또는 LLM) → **FR-7/8** 트리 라우팅 강화.
8. 벡터 백엔드 교체: numpy → **turbovec**(또는 sqlite-vec), abi3 휠 번들 + **AVX2 미지원/미설치 시
   자동 강등**(FR-14). 임베딩 오프라인화: OpenAI → **model2vec**(선택).

---

## 6. 설계 원칙 (기존 규약과 정합)

1. **원본 불변.** 관련정보·트리·임베딩은 전부 `data/` 사이드카에 축적. vault 원본은 읽기 전용.
2. **실시간 스트림 보호 최우선.** 신규 로직도 논블로킹·예외 무전파 원칙(`offer_segment` 계약) 유지.
3. **기본 안전.** 무거운 것(트리 생성·LLM 재랭킹·클라우드 벡터)은 플래그로, 실시간엔 가벼운 경로만.
4. **웹/CLI 공용 로직은 `wiki_core` 1곳.** `realtime_search.py`를 확장하되 UI별 분기는 호출자에.
5. **추적 가능성.** 모든 표시 결과는 출처 경로/노드/점수를 동반(위키 CLAUDE.md 출처 표기 규약과 일치).

---

## 7. 수용 기준 (Acceptance) — 판정 2026-07-29

범례: ✅ 충족(코드+자동테스트 489 passed/1 skipped) · ⚠️ 구현O·**라이브 미검증** · ❌ Phase 1.
(4·5·11은 v1.0에서 ✅였으나 리뷰에서 실효 없음이 드러나 수정 후 재확인한 항목이다 — 위 '구현 현황' 참고.)

**기능·표시**
1. ⚠️ Settings에서 "실시간 볼트 검색" ON → 실제 녹음 중 Recorder에 관련 노트가 뜨고,
   백엔드 미연결 시 **사유 배지**가 표시된다. (FR-1/2)
   → 기본값 ON·배지·5가지 사유 코드 구현 완료. **마이크 실녹음 확인만 남음.**
2. ✅ 관련 노트 클릭 → 근거 snippet·점수·섹션경로 확인 및 노트 이동 가능. (FR-3)
   → 근거는 '근거 보기'로, **노트 이동은 종료 후 회의 상세에서**(녹음 중 이동은 녹음 중단이라 의도적 제외).
3. ⚠️ 관련정보 표시가 **전사 본문을 가리거나 밀어내지 않고**, 팝업·포커스 이동·소리가 없다.
   내부(📄/🎓)와 웹(🌐) 출처가 배지로 구분되고 내부가 앞에 온다. (FR-10)
   → 고정 높이 바로 리플로우 0 구현. **사용성은 실녹음 중 체감 확인 필요.**

**검색 우선순위 (요청 핵심)**
4. ✅ 동일 발화에서 **내부자료(.md/논문 노트)가 웹보다 먼저·상위**에 뜬다. 웹은 내부에서 못 찾았을 때만,
   기본 off. (FR-11) → `realtime_web_only_if_no_vault_hit` 기본 true + `sortRelated`로 내부 앞줄.
5. ✅ 로컬 논문/원문추출(`02_이론_학습`·`01_References`·`원문추출/`)이 있으면 **웹 arXiv보다 먼저**
   인용된다. (FR-11) → 논문 한정 arm으로 후보 진입 보장(점수·순위 우대는 없음).
   **리뷰 수정 후 재확인**: 이 볼트에서 `02_이론_학습`·`원문추출`은 수정 전 arm 회수 0건이었고
   지금은 각각 4건. 임의 질의 3건에서 후보에 든 하위폴더 논문 0→6, 0→5, 0→0건.
6. ⚠️ en 발화로 관련 **한국어** 노트가, ko 발화로 관련 영어 자료가 상위에 검색된다(교차언어 스모크). (FR-13)
   → 회수는 확인, **정량 판정 보류**(영→한 R@3 .62). 환언 평가셋이 선행 조건.

**환경·유형**
7. ⚠️ `dist` 포터블(웹 `MeetingMinutes.bat`)과 소스 **터미널 CLI(`run_meeting.py realtime`)** 양쪽에서
   실시간 관련정보가 동작한다. turbovec 미설치 PC에선 TF-IDF+OpenAI 경로로 **자동 강등**되어 기능이
   멈추지 않는다. (FR-14) → 양쪽 배선 완료(공용 모듈 1곳). turbovec을 아예 안 쓰므로 강등 조건 자체가
   없다(현행이 강등 경로). 실기 확인만 남음.
8. ✅ meeting·seminar·lecture **세 유형 모두**에서 동일하게 동작한다(문서유형 게이트 없음 확인).
9. ✅ CLI 실시간 웹검색 동작이 (a) 구현되거나 (b) "웹 UI 전용"으로 문서화되어, 사용자가 혼동하지
   않는다. (FR-12) → (b) 채택, 5곳에 명시.

**누적·정합**
10. ✅ 회의 종료 후에도 관련 노트가 **근거와 함께 남아** 다시 열람 가능하고, 교차 회의 집계가 보인다. (FR-4/5)
    → 단, 서버(웹) 경로 녹음에만 해당. 프런트 단독/모바일 직접 연결은 사이드카를 쓰지 않는다.
11. ✅ 회의록에 `🔗 관련 노트`가 근거 링크와 함께 자동 삽입된다. (FR-6)
12. ❌ 긴 노트에 ToC 트리 + 노드 요약이 생성되고 실시간 결과에 섹션 경로가 실린다. (FR-7/8)
    → **섹션 경로는 실린다**(후보 내 위치특정, heading 정확도 .87). **ToC 트리·노드 요약은 Phase 1.**
13. ⚠️ 위 전 과정에서 **실시간 전사 지연·누락이 없다**(스트림 무영향 — 최우선).
    → 계약(논블로킹·예외 무전파)은 단위 테스트로 덮었고, 검색 1회는 로컬 89ms + 임베딩 API
    왕복 ~270ms = **실측 0.3~0.5초**(전용 워커 스레드). 발화 간격 9~15초·3세그먼트마다이므로
    여유가 있다. **실녹음 체감 확인 필요.**

---

## 8. 오픈 이슈 / 결정 필요

**해소된 것 (Phase 0)**
- ~~Phase 0 저장소~~ **결정·구현**: SQLite `related_notes` 테이블(session_id+note_path upsert).
- ~~FR-12 CLI 웹검색~~ **결정**: (b) 웹 UI 전용 + 문서화, `realtime_web_only_if_no_vault_hit` 신설.
- ~~검색 순서(섹션 우선 여부)~~ **실측으로 결정**: 노트 RRF 주축 + 논문 arm, 섹션은 후보 내 위치특정(§5.2).

**남은 것**
- **최우선 후속 — 환언 평가셋.** 합성 쿼리(축자 발췌) 편향 때문에 임베딩·교차언어 이득을 판정할 수
  없다. 실제 전사 발화로 만든 환언 평가셋이 있어야 FR-13과 Phase 1 임베딩 교체를 논할 수 있다.
- **FR-2 라이브 마이크 검증** — 사람 손이 필요한 유일한 잔여 항목(`TODO.md` 미해결).
- **Phase 0 저장소 확장:** Phase 1에서 벡터를 turbovec(`data/*.tvim`)로 뺄 때 회의 id·node_id를
  uint64로 일관 매핑.
- **임베딩 공급자:** Phase 0는 OpenAI(`text-embedding-3-small`) 유지(이미 동작). 오프라인 요구 시 Phase 1에서
  model2vec(정적·초경량, 54KB 휠) 검토. turbovec `dim`/`bit_width`는 선택 임베딩 차원에 맞춤.
- **트리/노드 요약 생성 트리거:** `reindex` 일괄 vs ingest 증분 — Phase 1 결정.
- ~~turbovec 휠 가부~~ **해소:** `cp39-abi3-win_amd64` 휠 존재 확인(3.13 설치 OK). 잔여 리스크는
  **AVX2 미지원 구형 CPU** 뿐 → FR-14 자동 강등으로 커버.
- PageIndex는 **의존성 강제 아님**(§5.0). 도입 시 자체호스팅/Flash(로컬·LLM-free) 우선, API/MCP는 대외비 반출 정책 확인.

---

## 9. 냉철한 검토 — 이 PRD의 리스크/함정 (Phase 0 후 실측 반영)

0. **실제로 현실화된 함정: 실측 없는 랭킹 휴리스틱.** 최초 구현은 "섹션 융합 + 논문 1.2배 가산"을
   그럴듯하다는 이유로 넣었고, 벤치를 만들자 **둘 다 반박**됐다(MRR .920→.713 / 지연 2.7배). 교체 커밋
   `64c3819`. 교훈: **검색·랭킹의 상수와 구조는 실측 후에 정하고, 측정 한계까지 문서에 남긴다.**
0-b. **두 번째로 현실화된 함정: "구현했다"와 "동작한다"의 간극.** 반박된 가산을 대체한
   "논문 arm이 후보 진입을 보장한다"는 안 자체가 **폴더 3개 중 1개에서만 작동**하고 있었다
   (접두사 vs 경로중간 일치 규칙이 두 곳에 따로 구현돼 갈라짐). 벤치도 같은 호출을 써서
   숫자로 드러나지 않았고, 단위 테스트의 fake 인덱서는 규칙을 자체 구현해 실제 버그를
   덮었다. 교훈 셋: **① 같은 판정 규칙을 두 곳에 손으로 쓰지 않는다**(공용 헬퍼),
   **② fake 는 실제 코드가 쓰는 헬퍼를 호출한다**, **③ 랭킹 대체안도 "실제 볼트에서 몇 건
   회수되나"를 세어 검증한다** — 지표만 보면 이 arm 은 상위 3개에 거의 영향이 없어 조용히
   죽어 있어도 티가 나지 않는다.
1. **과설계 경계.** turbovec/PageIndex를 v1에 넣으면 "472개 노트에 ANN·트리 파이프라인"이라는 과잉이 된다.
   → §5.0대로 **Phase 0는 기존 스택으로**(로컬 계산 실측 89ms), 두 라이브러리는 규모/오프라인 트리거가 실제로
   생길 때만.
2. **실시간 임베딩 지연·비용.** 세그먼트마다 OpenAI 임베딩 호출 시 지연·비용 누적. → 쿼리 임베딩은
   스로틀(현행 3세그먼트) + 짧은 쿼리 + 캐시. 노트 임베딩은 사전 계산(현행 `build_embeddings`) 재사용.
3. **오프라인이면 Phase 0가 안 돈다.** Phase 0 임베딩·웹검색은 API 의존. 완전 에어갭 요구가 실제라면
   model2vec 로컬 임베딩이 Phase 0 필수가 됨 → 요건 우선 확인.
4. **교차언어 한계(FR-13).** 완벽 보장 아님. 스모크 기준선 미달 시 다국어 임베딩 교체가 진짜 해법.
5. **비방해 표시의 실측.** "방해 안 함"은 주관적 → 수용기준을 "본문 리플로우 0 / 팝업·포커스이동·소리 0"로
   객관화(§7-3)했으나, 실제 녹취 중 사용성 테스트로 확인 필요.
6. **CLI 웹검색 불일치(FR-12).** 구현 대신 "웹 전용 명시"만 택해도 되지만, 사용자 기대와 어긋나지 않게 문서화 필수.
