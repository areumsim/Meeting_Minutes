# ── 비밀 설정 (절대 git에 올리지 말 것) ─────────
config.json
.env

# ── 출력 파일 ─────────────────────────────────────
output/
*.log

# ── Python ────────────────────────────────────────
__pycache__/
*.pyc
*.pyo
*.pyd
.Python

# ── 임시 파일 ─────────────────────────────────────
*.tmp
*.bak
temp_*

# ── 오디오 백업 (크래시 복구용 대용량 파일) ─────────
# output/ 전체가 이미 무시되므로 아래는 명시적 문서화 목적
*.pcm
*.s16le
