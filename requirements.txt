# Required
# GA Realtime API(client.realtime, session.type='transcription')는 openai>=1.107 필요.
openai>=1.107.1
httpx>=0.27.0

# WebSocket streaming (--mode ws, lower latency ~1s)
websockets>=13.0

# Optional (Claude fallback)
anthropic>=0.30.0

# Realtime recording
sounddevice>=0.4.6
numpy>=1.24.0

# Notifications
requests>=2.31.0

# File watcher
watchdog>=4.0.0

# Web UI (FastAPI backend)
fastapi>=0.110.0
uvicorn[standard]>=0.29.0
python-multipart>=0.0.9
# 회사망 SSL 검사 대응 — OS(Windows) 인증서 저장소 신뢰 (web/backend/app.py에서 주입)
truststore>=0.10

# Optional (VAD mode - faster response on short utterances)
# Windows: pip install webrtcvad-wheels
# Mac/Linux: pip install webrtcvad
# webrtcvad-wheels>=2.0.10
