# constraints-web.txt — 포터블 배포본이 설치할 **정확한** 버전 조합 (pip constraints)
# =============================================================================
# 왜 필요한가 — requirements-web.txt 는 `>=` 범위라 pip 이 **빌드 시점에** 버전을 정한다.
# 그래서 같은 커밋을 다시 빌드해도 다른 조합이 나오고, 더 나쁜 것은 **테스트가 검증한
# 조합과 배포되는 조합이 다르다**는 점이다. 2026-08-03 실측:
#
#     패키지      개발환경(테스트가 도는 곳)   배포본(사용자가 받는 것)
#     uvicorn     0.41.0                      0.52.1
#     fastapi     0.135.1                     0.141.1
#     openai      2.46.0                      2.52.0
#     pydantic    2.12.5                      2.13.4
#     anthropic   0.118.0                     0.120.2
#     numpy       2.2.2                       2.5.1
#
# 프런트엔드는 package-lock.json 으로 이미 고정돼 있었고 파이썬 쪽만 열려 있었다.
#
# 여기 적힌 조합은 **실기 검증을 통과한 조합**이다(포터블 481d05a 빌드: UI 서빙,
# /ws/realtime 핸드셰이크, 휴지통·되돌리기·완전삭제, 정상 종료, 임베디드 파이썬 import).
#
# 갱신 방법 — 의존성을 올리려면 의식적으로 해야 한다:
#   1) requirements-web.txt 의 범위를 필요한 만큼 넓히고
#   2) 이 파일을 지운 뒤 빌드해 새 조합을 얻고(빌드가 경고와 함께 진행된다)
#   3) 새 조합으로 실기 검증을 다시 하고 이 파일을 갱신한다.
# 전이 의존성까지 고정하므로, 충돌이 나면 pip 이 **크게 실패**한다 — 그것이 의도다
# (조용히 다른 조합이 배포되는 것보다 낫다).
#
# 주의: 이 파일은 배포 산출물 전용이다. 개발 설치(`pip install -e .`)는 pyproject 의
# 범위를 쓰며, 그래서 dev 와 dist 가 갈라질 수 있다 — 배포 전 검증은 배포본으로 한다.
# =============================================================================

annotated-doc==0.0.5
annotated-types==0.8.0
anthropic==0.120.2
anyio==4.14.2
av==18.0.0
certifi==2026.7.22
charset_normalizer==3.4.9
click==8.4.2
colorama==0.4.6
ctranslate2==4.8.1
distro==1.9.0
docstring-parser==0.18.0
fastapi==0.141.1
faster-whisper==1.2.1
filelock==3.32.2
flatbuffers==25.12.19
fsspec==2026.7.0
h11==0.16.0
hf-xet==1.5.2
httpcore==1.0.9
httptools==0.8.0
httpx==0.28.1
huggingface-hub==1.26.0
idna==3.18
jiter==0.16.0
numpy==2.5.1
onnxruntime==1.28.0
openai==2.52.0
packaging==26.2
protobuf==7.35.1
pydantic==2.13.4
pydantic-core==2.46.4
python-dotenv==1.2.2
python-multipart==0.0.32
pyyaml==6.0.3
requests==2.34.2
Send2Trash==2.1.0
setuptools==83.0.0
sniffio==1.3.1
starlette==1.3.1
tokenizers==0.23.1
tqdm==4.70.0
truststore==0.10.4
typing-extensions==4.16.0
typing-inspection==0.4.2
urllib3==2.7.0
uvicorn==0.52.1
watchdog==6.0.0
watchfiles==1.2.0
websockets==15.0.1
